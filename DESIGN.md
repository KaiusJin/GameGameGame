# 设计文档 v2 · 二手电脑（重构版）

> 本文档是唯一的设定源。改剧情 = 改 `js/data.js`，不改代码。
> 2026-09-01 按《重构简报 v1》改版。旧版（远程协助救女儿）全部代码与文案见 git 历史 `aa39fb9`。

## 一句话

你买了一台二手电脑，里面有一个删不掉的黑客软件，它不断给你派"求助单"。
你救下的人都会意外死亡。三个人之后，你明白唯一的救法是不用它。

## 与旧版的取舍

| | 内容 |
|---|---|
| 删 | 远程协助外框、母亲聊天坞、"连女儿电脑"框架、一切超自然解释、平台 SDK 机制（昵称/云存档/双端） |
| 留 | 假 Windows 桌面 + 假浏览器（已复用）、留学生骗局作第一案（反诈研究全部沿用，见文末附录）、"每屏一个亮点/灰态可见/解锁级联"引导、内容全走 JSON |
| 增 | 二手电脑开场（前任机主 txt 链）、病毒软件（黑绿终端，派单器+看别人设备的唯一入口）、三案结构（长/短/反）、死亡只在新闻页揭晓、第三案不用软件 |

## 剧情骨架（结构级；文案人设一律不写）

| 阶段 | 内容 | 状态 |
|---|---|---|
| 0 开机 | 二手电脑、txt 链、发现软件、删除失败、第一单弹出 | ✅ 纵切片 |
| 1 第一案 | 留学生虚拟绑架，完整流程，玩家要真的赢一次 | ✅ 骨架（占位文案） |
| 1.5 余波 | 几天后新闻页出现她的意外 | ✅ 纵切片 |
| 2 / 2.5 第二案 | 短。验证规律的实验，死法换一种 | 未做 |
| 3 第三案 | 软件失效或被拒用，只靠普通 app | 未做 |
| 4 结局 | — | 未做 |

**待 Aaron 确认**（简报原文，未拍板前不写进数据）：
1. 死亡机制的解释（推荐：求助名单就是找人名单，幕后是人，玩家是他们的搜索引擎）
2. 三个人之间的联系（推荐：前任机主链，每一任都"帮"过一个人）
3. 结尾选项（可选：第三张求助单上是玩家自己）

## 架构：先框架，后内容

```
index.html            单页：开机遮罩 + 桌面 + 全部窗口（预置，display 切换）
js/engine/state.js    状态机：flags + phase + triggers + 游戏内时钟 + 存档（六条设计规则写在此文件头部注释）
js/engine/wm.js       窗口系统：多窗口/拖动/最小化/任务栏 + [data-t] 文案应用 + [data-show-flag] 显隐
js/engine/notify.js   通知：toast / 任务栏闪烁 / 图标红点 / 亮点(hotspot)引擎
js/engine/boot.js     开机序列（首次完整播放，此后快进）
js/apps/files.js      ① 文件管理器：虚拟文件树 + 桌面文件图标
js/apps/browser.js    ② 浏览器：JSON 页面 / 新闻站（独立数据源）/ 历史记录 / 可输入地址栏
js/apps/chat.js       ③ 聊天：自己的会话 + 监控模式（只读）
js/apps/viewer.js     ④ 查看器：txt / 图片（假元数据面板）/ 红头文书
js/apps/virus.js      ⑤ 病毒软件：求助单 / 目标档案 / 接入目标设备 / 永远失败的卸载 / 矛盾的关于页
js/data.js            唯一剧情源（文案表 TEXT + 下述数据模型）
css/system.css        系统层：toast/红点/亮点/灰态/监控条/新浏览器视图
css/virus.css         黑绿终端
css/pc.css apps.css browser.css   旧版复用（桌面/窗口/微信壳/文书/Chrome 壳）
```

**关键机制**
- 一切由状态机驱动：app 只发事件（`STATE.emit`）、读 flag、渲染数据。触发器 = 条件→动作（set/toast/badge/open/dialog/advance/source/delay），全部一次性，存档后不重放。
- "接入目标设备" = 给文件/浏览器/聊天/查看器切数据源（`STATE.setSource("t1")`），顶部出现绿色监控条，退出即回 own。桌面永远是玩家自己的。
- 游戏内时钟与现实脱钩：剧情用 `["advance", 天数]` 推进（"几天后"），任务栏时钟只作展示。
- 存档：localStorage，接口收敛在 `state.js` 的 `STORE`（get/set/clear），可整体换云端。
- 引导三件套：toast 指路 + 图标红点/任务栏闪烁 + 全局唯一亮点（`DB.HINTS` 首个命中条件的元素发绿光）。灰态可见：锁住的文件/记录显示但发灰，点击弹"无法打开"。

## 数据模型（js/data.js，全部 *Ref 指向 TEXT 文案表；缺失显示〔ref〕不报错）

```
TEXT      { ref: 文案 }                         全部界面文字，含按钮与报错（规则 6）
TRIGGERS  [ { id, on:"event:x"|"change", if:<cond>, do:[动作] } ]
HINTS     [ { if:<cond>, target:"data-hint 值" } ]   首个命中 = 当前唯一亮点
DEVICES   { own:{files,chats,history}, t1:{...} }    数据源；files/history 支持 locked(灰态)/sets(读后置 flag)
PAGES     [ { id, url, kind, titleRef } ]            地址栏命中列表内才有页面
NEWS      [ { id, headRef, dateRef, bodyRef, appears:<cond> } ]   死亡只在这里揭晓
TASKS     [ { id, target, steps:[{ref,done,locked}], complete, doneFlag } ]
OWNERS    [ { id, order, notes:[fileId] } ]          前任机主链（二三案钩子）
<cond> = {flag}|{not}|{phase}|{all}|{any}
```

## 设计规则（同步写在 state.js 头部注释）

1. 黑客解释必须始终成立：任何异常都能用"有人在操作"解释。
2. 软件来历只以三个互相矛盾的来源出现：关于页（来历①）、argus_setup.exe 文件属性（来历②）、前任 txt（来历③）。永不给标准答案。
3. 死亡不演，只通过新闻页被玩家刷到。
4. 前十分钟一条直线；每屏一个亮点；软件派单就是教程。
5. 不出现真实黑客工具名与真实手法；界面黑绿到底，内容好莱坞式。
6. 页面不写死任何文字（按钮、报错在内），全走 TEXT。

## 第一周纵切片（已跑通，2026-09-01 浏览器实测）

开机 → 读 txt 链（3 封，逐封解锁）→ ARGUS_9 图标出现 → 卸载失败（0x00000709）→
求助单 #001 弹出 → 接入目标设备（监控条+只读微信）→ 聊天 → 解锁浏览记录 → 解锁下载文书 →
回软件回报 → 任务完成 → 时钟 +3 天 → 新闻站红点 → 《一名留学生意外身亡》→ 纵切片终点弹窗。
全程由 toast/红点/亮点指路，重进不丢档、不重播触发器。

## 附录 · 沿用的反诈研究（第一案正式文案的素材库）

**骗局三招**（不发明新招）：①洗脑：每天固定时段视频"汇报"；②伪造场景：假《刑事拘留通知书》《保密承诺书》+假警官证；③制造失联："案件保密"为由切断一切联系、不许报警。
**官方三句**（以线索形式出现，不说教）：公检法不会通过电话/微信办案；不存在"安全账户"；法律文书不会网络发给本人。
**假文书破绽**（real 读法实证）：送达对象是本人；出现"逮捕令"（不存在的文书）；文号格式错误；"唯一指定安全核查账户"话术；承诺书要求统一回复。
完整两个月时间线、双读法线索表、全部成稿文案见 `aa39fb9` 的 DESIGN.md 与 data.js。

## 不做

剧情文案、人物设定、真实黑客技术、平台 SDK、正式美术资产。
